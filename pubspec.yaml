name: entry_kit
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.3

  # Value Equality (Prevents unnecessary rebuilds)
  equatable: ^2.0.5

  google_sign_in: ^6.1.5

  sign_in_with_apple: ^6.1.0

  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # For testing Cubits/Blocs
  bloc_test: ^9.1.5

  # For mocking the AuthRepository
  mocktail: ^1.0.1


flutter:
  assets:
    - assets/google_logo.svg